cargo-features = ["profile-rustflags"]

[package]
name = "maestro"
version = "0.1.0"
authors = ["llenotre <llenotre@student.42.fr>"]
edition = "2021"

[lib]
name = "ps2"
path = "src/mod.rs"
crate-type = ["dylib"]

[profile.release]
lto = true
panic = "abort"
